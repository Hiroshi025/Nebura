<style>
  .admin-footer {
    --footer-bg: var(--card-bg-dark);
    --footer-text: var(--text-color-dark);
    --footer-border: var(--input-border-dark);
    --footer-accent: #6366f1;
    --footer-hover: #818cf8;
    --footer-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);

    position: relative;
    z-index: 1000;
    background: rgba(30, 30, 45, 0.85);
    /* translúcido y oscuro */
    color: var(--text-color-dark);
    border-top: none;
    box-shadow: 0 -2px 16px 0 rgba(0, 0, 0, 0.06);
    padding: 1rem 2rem;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 56px;
    font-size: 0.95rem;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    margin-top: 20px;
  }

  .admin-footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--footer-accent), transparent);
    opacity: 0.3;
  }

  .footer-left {
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }

  .footer-left img {
    height: 36px;
    width: 36px;
    border-radius: 10px;
    margin-right: 0.8rem;
    background: #fff;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    transition: var(--footer-transition);
  }

  .footer-left img:hover {
    transform: rotate(5deg) scale(1.05);
  }

  .footer-left .divider {
    width: 1px;
    height: 24px;
    background: var(--footer-border);
    margin: 0 0.5rem;
    opacity: 0.5;
  }

  .footer-links {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .footer-links a {
    color: var(--footer-text);
    text-decoration: none;
    opacity: 0.7;
    font-weight: 500;
    position: relative;
    transition: opacity 0.2s;
    padding: 0.3rem 0;
  }

  .footer-links a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--footer-accent);
    transition: var(--footer-transition);
  }

  .footer-links a:hover {
    opacity: 1;
    color: var(--footer-hover);
  }

  .footer-links a:hover::after {
    width: 100%;
  }

  .version-badge {
    background: linear-gradient(135deg, var(--footer-accent), #7c3aed);
    color: white;
    padding: 0.3rem 0.7rem;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.5px;
    box-shadow: 0 2px 10px rgba(99, 102, 241, 0.2);
    transition: var(--footer-transition);
  }

  .version-badge:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3);
  }

  @media (max-width: 768px) {
    .admin-footer {
      flex-direction: column;
      gap: 1rem;
      padding: 1rem;
      font-size: 0.9rem;
      text-align: center;
    }

    .footer-left {
      flex-direction: column;
      gap: 0.5rem;
    }

    .footer-left .divider {
      display: none;
    }

    .footer-links {
      flex-wrap: wrap;
      justify-content: center;
      gap: 1rem;
    }
  }

  [data-bs-theme="light"] .admin-footer {
    background: rgba(255, 255, 255, 0.85);
    color: #555;
  }
</style>

<footer class="admin-footer w-100 mt-auto">
  <div class="footer-left">
    <div class="d-flex align-items-center">
      <img src="/images/logo.png" alt="Nebura Logo">
      <span class="fw-semibold" style="letter-spacing: 0.5px;">Nebura Plataforma</span>
    </div>
    <div class="divider"></div>
    <span class="text-muted" style="opacity: 0.7;">&copy; <%= new Date().getFullYear() %> All rights reserved</span>
  </div>

  <div class="footer-links">
    <a href="/dashboard/support">Soporte</a>
    <a href="https://help.hiroshi-dev.me/" target="_blank">Documentación</a>
    <a href="#">Términos</a>
    <a href="#">Privacidad</a>
    <span class="version-badge ms-2">v0.0.9</span>
  </div>
</footer>